torch>=1.10.0
torchvision>=0.11.1
torchaudio>=0.10.0
segmentation-models-pytorch>=0.3.0 # Excellent library for segmentation models (U-Net, DeepLab, etc.)
albumentations>=1.1.0             # For data augmentation
pyyaml>=6.0                      # For loading training config
tqdm>=4.60.0                     # Progress bars
scikit-learn>=1.0.0              # For metrics (optional)
tensorboard>=2.8.0               # For logging training progress (optional)
imageio # Already needed for GIF saving in generator
# Add other specific libraries as needed