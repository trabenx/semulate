import numpy as np

# Initially might be empty or contain simple helpers.
# Added scale_to_uint here instead of io_utils if preferred.
# from .io_utils import scale_to_uint # Or define here

# Example function (already in io_utils, keep in one place)
# def scale_to_uint(img: np.ndarray, dtype: Union[np.uint8, np.uint16]) -> np.ndarray: ...

# Add other general image processing utilities here as needed.
# E.g., normalization, contrast adjustment, etc.